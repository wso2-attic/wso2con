<%
var db = application.get('database');
if (db == null) {
    db = new Database("jdbc:mysql://localhost:3306/--ADD HERE--", "--ADD HERE--", "--ADD HERE--");

    //db.query("SET autocommit = 0;");
    application.put('database', db);
}

%>
