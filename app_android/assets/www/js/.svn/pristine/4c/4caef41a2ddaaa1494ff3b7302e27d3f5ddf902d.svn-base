// Prepare the map view
// todo: update with better map, enable 2-axis scrolling
$('#map').live('pageshow', function(event){
    if (testmode) { console.log("pageshow #map"); }
    myScroll = new iScroll('mapscroll', { zoom: true, lockDirection: false });
});

